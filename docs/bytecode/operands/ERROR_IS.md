# ERROR_IS

## Syntax

`ERROR_IS <dst>, <error>, <type>`

## Behavior

Writes `true` if `<error>` has the supplied type string. `Exception` matches any non-empty error type.

Requires `.import <exception>`.
