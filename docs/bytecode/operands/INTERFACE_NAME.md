# `INTERFACE_NAME` Instruction

Sets the name of an interface value.

## Category

- `interface`

## Syntax

```aic
INTERFACE_NAME <interface>, <name>
```

## Behavior

Updates `<interface>` with the string name `<name>`.

## Errors

Raises an error if `<interface>` is not an interface or `<name>` is not a string.
