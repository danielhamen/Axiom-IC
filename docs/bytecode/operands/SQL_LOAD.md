# `SQL_LOAD` Instruction

Loads row data for SQL-style querying.

## Category

- `sql`

## Syntax

```
SQL_LOAD <dst>, <rows>
```

## Behavior

Copies a list of map rows into `<dst>`. Rows are ordinary AIC map values.
