# `JSON_FILE_LOAD` Instruction

Loads and parses a JSON file.

## Category

- `json`

## Syntax

```
JSON_FILE_LOAD <dst>, <path>
```

## Behavior

Reads the file at `<path>`, parses it as JSON, and writes the resulting AIC value to `<dst>`.
