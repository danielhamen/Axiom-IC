# `REG_REPLACE` Instruction

Replaces regex matches in a string.

## Category

- `regex`

## Syntax

```
REG_REPLACE <dst>, <text>, <pattern>, <replacement>
```

## Behavior

Writes a string where every regex match in `<text>` is replaced by `<replacement>`.
